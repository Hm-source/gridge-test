jwt:
  secret: ${JWT_SECRET}
  token:
    expiration: 86400  # 24시간 (초)
  refresh-token:
    expiration: 604800  # 7일 (초)


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3309/gridgestagram?useSSL=false&allowPublicKeyRetrieval=true
    username: kim
    password: 1234

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
